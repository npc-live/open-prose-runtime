{
  "name": "open-prose",
  "version": "0.1.0",
  "description": "OpenProse - A DSL for orchestrating AI agent sessions",
  "type": "commonjs",
  "main": "src/index.ts",
  "bin": {
    "open-prose": "bin/open-prose.ts"
  },
  "scripts": {
    "compile": "bun run bin/open-prose.ts compile",
    "validate": "bun run bin/open-prose.ts validate",
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "bun": ">=1.0.0"
  },
  "dependencies": {
    "dotenv": "^17.3.1"
  }
}
